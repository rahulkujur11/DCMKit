import logging
logger = logging.getLogger(__name__)

def transform_capacity_silver(input_data):
    logger.debug(f"Transforming capacity data: {input_data}")
    return input_data

# === TRANSFORM FUNCTION ===
def transform_comment_silver(input_data):
    logger.debug(f"Transforming comments data: {input_data}")
    return input_data
    

def transform_demand_silver(input_data):
    logger.debug(f"Transforming demand data: {input_data}")
    return input_data

